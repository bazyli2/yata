# Backend Postgres connection (TCP). Defaults below match the devbox
# Postgres instance, so backend/.env can normally stay empty.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=app

# Comma- or JSON-style list of allowed CORS origins.
CORS_ORIGINS=["http://localhost:3000"]
